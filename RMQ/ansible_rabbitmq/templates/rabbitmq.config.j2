[
  {rabbit, [
     {ssl_listeners, [5671]},
     {ssl_options, [{cacertfile, "{{certificate_dest}}ca_certificate.pem"},
                    {certfile,   "{{certificate_dest}}server_certificate.pem"},
                    {keyfile,    "{{certificate_dest}}server_key.pem"},
                    {verify,     verify_peer},
                    {fail_if_no_peer_cert, true}]}
   ]}
].
